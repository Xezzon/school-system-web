import React from 'react';

/**
 * 登录对话框
 * 收到401响应后自动弹出
 * 不允许以任何方式手动关掉对话框
 * 登录成功后销毁
 */
function AuthenticationModal({ visible }) {
    return <React.Fragment></React.Fragment>
}

export default AuthenticationModal;
